package q1110;

import java.util.Scanner;

/*
 * public class Main {
 * 
 * public static void main(String[] args) { Scanner sc = new Scanner(System.in);
 * String oldStr = sc.next(); sc.close(); String nuwStr; int count = 0;
 * 
 * while(true) { if (oldStr.length() > 10) { int oldNum =
 * Integer.parseInt(oldStr); newStr = oldStr.charAt(1) + oldNum / 10 + oldNum %
 * } } }
 * 
 * }
 */
